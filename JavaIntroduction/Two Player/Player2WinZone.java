import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Player2WinZone here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Player2WinZone extends Actor
{
    /**
     * Act - do whatever the Player2WinZone wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {

    }    
      public Player2WinZone()
    {
        getImage().scale(10, 600);
               getImage().setTransparency(0);
    }
}
